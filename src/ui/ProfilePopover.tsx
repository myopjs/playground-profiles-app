import {MyopComponent} from "@myop/react";
import {COMPONENTS_IDS} from "../utils/componentsIds.ts";


export const ProfilePopover = () => {

    return  <MyopComponent componentId={COMPONENTS_IDS.profilePopover} />
}